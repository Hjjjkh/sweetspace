{
  "name": "love-space-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "init-db": "wrangler d1 execute love-space-db --local --file=./schema.sql",
    "init-db:prod": "wrangler d1 execute love-space-db --file=./schema.sql",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "wrangler": "^3.105.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
